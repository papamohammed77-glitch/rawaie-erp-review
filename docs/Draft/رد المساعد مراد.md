# RAWAEA ERP — Manual Voucher Lifecycle Completion

## Reconciliation Gate — NO GO

تم تنفيذ التكليف حتى نقطة **Root Cause → Complete Reconciliation**، ولم يتم تعديل أي ملف أو تنفيذ SQL أو تغيير Production.

---

# 1. FACTS CONFIRMED

### 1.1 Production Schema

`stock_vouchers` في الـProduction Evidence يحتوي على:

`id, company_id, voucher_code, voucher_date, type, status, from_branch_id, to_branch_id, from_type, from_id, to_type, to_id, reference, notes, created_by, sent_date, received_date, completed_at, created_at, updated_at, source`

ولا يحتوي على:

`completed_by`.

---

### 1.2 Production `complete_manual_stock_voucher_atomic`

الـRPC المنشورة فعليًا تنفذ:

```sql
status = 'Completed'
completed_at = now()
completed_by = p_user_email
```

وبالتالي تحاول الكتابة في عمود غير موجود في Production Schema. هذا **Production RPC defect مثبت** وليس Test Harness defect.

---

### 1.3 Current Edge Function

`complete-stock-voucher/index.ts` لا يكتب `completed_by` مباشرة.

هو يستدعي:

```text
complete_manual_stock_voucher_atomic(
    companyId,
    voucherCode,
    user.email
)
```

وبالتالي نقطة الخطأ الفعلية تقع داخل الـRPC المنشورة، وليس في الـEdge Function الحالية.

---

### 1.4 Current CREATE

الـCurrent CREATE:

* يتحقق من صلاحية المستخدم.
* يحدد `companyId`.
* يتحقق من الفروع داخل الشركة.
* يتحقق من أنواع الأذونات.
* يستدعي `create_manual_stock_voucher_atomic`.
* لا يعدل المخزون عند الإنشاء.
* ينشئ الحالة `Draft`.

والـRPC نفسها تتحقق من الشركة، نوع الإذن، الفروع، الأصناف، الكميات، وعدم تكرار الصنف داخل الإذن.

---

### 1.5 Current SEND

الـCurrent SEND:

```text
Draft
 ↓
post_manual_stock_voucher_atomic(SEND)
 ↓
Sent
```

ويستخدم `buildSendEffects()` التي تنتج حركة:

```text
OUT
```

من `voucher.from_id`.

الـProduction RPC المنشورة تقفل الـvoucher بـ`FOR UPDATE`، وتقفل صف `stock_branches` بـ`FOR UPDATE`، وتتحقق من:

```text
qty - allocated_qty
```

ثم تحدث `qty` وتكتب `inventory_log` داخل نفس العملية.

---

### 1.6 Current RECEIVE

الـCurrent RECEIVE:

* يسمح فقط عندما تكون الحالة `Sent`.
* يستطيع الاستلام الجزئي.
* يحسب الكمية المتبقية من `qty - received_qty`.
* يبني حركة `IN` إلى `voucher.to_id`.
* يستدعي نفس الـatomic posting RPC.
* تصبح الحالة `Received` فقط عندما لا تبقى كمية غير مستلمة.

والـProduction RPC تحدث `received_qty` وتكتب `inventory_log` وتحدث حالة الإذن في نفس العملية.

---

### 1.7 Current COMPLETE

الحالة المتوقعة:

```text
Transfer / DirectReturn → Received
DirectSale / SupplierReturn → Sent
```

ثم:

```text
Completed
```

والـComplete لا يحتوي في الـEdge Function على أي Inventory mutation.

---

### 1.8 Current CANCEL

الـCurrent Edge Function تستدعي:

```text
cancel_manual_stock_voucher_atomic
```

ولا تنفذ Inventory mutation بنفسها.

والـMigration المستهدفة تنص على أن CANCEL يسمح فقط للـ`Draft`، وأن الإذن الذي نفذ حركة مخزنية لا يُلغى بل يحتاج حركة عكسية رسمية.

---

# 2. UNKNOWN / UNPROVEN

### A. هل `completed_by` يجب أن يوجد في Target؟

**UNKNOWN / TARGET DECISION REQUIRED**

المشكلة المثبتة هي أن Production لا يحتوي العمود، لكن لا يجوز حذف مفهوم Actor Audit تلقائيًا.

الـArchitecture Constraints تنص صراحة على ضرورة تحديد Target audit contract قبل إضافة/حذف حقول مثل `completed_by`.

---

### B. Target النهائي لـDirectSale

هناك تعارض حقيقي بين الأدلة:

**Architecture manual-voucher document:**

```text
DirectSale
source qty ↓
destination qty —
```

ويتم ذلك عند SEND.

بينما Migration `20260810_manual_voucher_core_v1_RELEASE.sql` تحتوي منطقًا مختلفًا:

```text
DirectSale SEND
OUT source
+
IN destination
```

وتتحقق من وجود الحركتين.

أما Production RPC الحالية فتتعامل مع SEND على أنه `OUT` فقط.

إذن:

**Target behavior = UNKNOWN / TARGET DECISION REQUIRED.**

---

### C. Target النهائي لـDirectReturn

هناك تعارض مماثل:

وثيقة الأذونات اليدوية تصف:

```text
DirectReturn
source qty ↑
```

عند SEND.

بينما Current implementation:

```text
RECEIVE
→ IN
→ voucher.to_id
```

والـMigration المستهدفة تحتوي منطقًا مختلفًا يسمح:

```text
OUT from vehicle
+
IN to destination
```

إذن:

**Target behavior = UNKNOWN / TARGET DECISION REQUIRED.**

---

### D. CANCEL Target الكامل

الـOriginal يسمح بالإلغاء طالما لم تكن الحالة `Completed` أو `Cancelled`.

لكن Migration Target الحالية تقصر CANCEL على `Draft`.

الفرق واضح، لكن هل هذا هو Target النهائي المعتمد معماريًا؟

**TARGET DECISION REQUIRED.**

---

# 3. DISCREPANCIES

| Source                                            | Actual                                                     | Expected                                                     | Severity                       |
| ------------------------------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------ |
| Production Schema vs Complete RPC                 | RPC تكتب `completed_by`                                    | يجب أن تتوافق RPC مع Production Schema/Target Audit Contract | **P0**                         |
| Architecture vs Migration — DirectSale            | Architecture: OUT فقط                                      | Migration: OUT + IN                                          | **P0 — Target conflict**       |
| Architecture vs Current/Production — DirectReturn | Architecture: حركة عند SEND                                | Current/Production: IN عند RECEIVE                           | **P0 — Target conflict**       |
| Original CANCEL vs Migration                      | Original يسمح إلغاء حالات أوسع                             | Migration تقصره على Draft                                    | **P1 — Target reconciliation** |
| Migration vs Production                           | Migration `20260810...` معلنة `NOT EXECUTED IN PRODUCTION` | Production definitions مختلفة                                | **P0**                         |
| Previous Assistant vs Production Evidence         | السابق قال إن RPC لا تستخدم `completed_by`                 | Production Evidence تثبت أنها تستخدمه                        | **P0 evidence contradiction**  |

المهم هنا أن **ليست كل الاختلافات Bugs**. بعضها اختلاف Target يحتاج قرارًا صريحًا.

---

# 4. ROOT CAUSE

السبب الجذري ليس مجرد `completed_by`.

السبب الجذري الأكبر الذي أثبته الـreconciliation هو:

> **وجود أكثر من تعريف لنموذج Manual Voucher Lifecycle في الوقت نفسه.**

لدينا حاليًا:

```text
Architecture Definition
        +
Original Functions
        +
Current Edge Functions
        +
Production RPC
        +
Unexecuted Target Migration
```

وهذه المصادر لا تتطابق بالكامل.

أوضح مثال:

```text
DirectSale
```

له ثلاثة سلوكيات مختلفة في هذه المصادر.

وكذلك:

```text
DirectReturn
```

له أكثر من نموذج.

إذن تعديل `completed_by` وحده ثم تشغيل الاختبار سيكون بالضبط نمط:

```text
Patch
→ Test
→ Discover another discrepancy
→ Patch
→ Discover another business rule
```

وهو النمط الذي يمنعه التكليف صراحةً. 

---

# 5. CORRECTIVE PLAN

ليس من الآمن كتابة Patch Lifecycle الآن.

التصحيح المطلوب على مرحلتين:

### المرحلة A — Target Reconciliation

تثبيت Target نهائيًا لكل:

```text
CREATE
SEND
RECEIVE
COMPLETE
CANCEL
```

وبالأخص:

```text
DirectSale
DirectReturn
SupplierReturn
Transfer
```

مع تحديد:

* الحالة قبل/بعد.
* الحركة المخزنية.
* اتجاه الحركة.
* الفرع المتأثر.
* `inventory_log`.
* Actor.
* Audit.
* Atomic boundary.

### المرحلة B — One Coherent Patch

بعد تثبيت Target فقط:

```text
Production Schema
        ↓
Production RPC
        ↓
Current Edge
        ↓
Original
        ↓
Migration
        ↓
Architecture
        ↓
One corrective implementation
```

وسيكون أول إصلاح واضح بعد الحسم هو معالجة `completed_by` بما يتفق مع **Target Audit Contract**، وليس بإضافة العمود تلقائيًا.

---

# 6. IMPACT ANALYSIS

إذا تم الإصلاح الصحيح، المسارات المتأثرة هي:

```text
vouchers.html / main.html
        ↓
CREATE
        ↓
SEND
        ↓
RECEIVE
        ↓
COMPLETE
        ↓
CANCEL
```

وتحديدًا:

* `stock_vouchers`
* `stock_voucher_details`
* `stock_branches`
* `inventory_log`
* Audit behavior
* Company isolation
* Inventory atomicity
* DirectSale custody behavior
* DirectReturn behavior

ولا يوجد دليل يسمح حاليًا بتغيير Schema أو إضافة `completed_by`.

---

# 7. VALIDATION PLAN

بعد حسم الـTarget، الاختبار يجب أن يكون:

```text
BEGIN
  ↓
Create controlled test state
  ↓
CREATE
  ↓
Validate
  ↓
SEND
  ↓
Validate stock + log + status
  ↓
RECEIVE
  ↓
Validate stock + received_qty + log + status
  ↓
COMPLETE
  ↓
Validate status + audit
  ↓
CANCEL scenario
  ↓
Validate
  ↓
ROLLBACK
```

ثم:

### Static reconciliation

```text
CURRENT >= ORIGINAL
```

بالنسبة إلى الـbehavior الذي يجب الحفاظ عليه، مع استثناء السلوك الذي يثبت أن الـTarget غيّره عمدًا.

### Production validation

يجب إثبات:

* Production Schema.
* Production RPC signatures.
* Production deployed definitions.
* كل lifecycle state.
* Inventory movement exactly once.
* Inventory log exactly once.
* Audit contract.
* Failure rollback.
* Company context.
* Self-cleaning test.

والـGO Gate لا يسمح بـGO مع بقاء أي شرط غير مثبت. 

---

# الحالة الحالية

## **NO GO**

وليس بسبب `completed_by` فقط.

السبب الحقيقي:

> **يوجد الآن Lifecycle Business Rule reconciliation غير مكتمل، وبالأخص DirectSale وDirectReturn، بالإضافة إلى Production RPC mismatch المؤكد في `completed_by`.**

ولذلك **لن أكتب Patch الآن**؛ لأن ذلك سيكون مخالفًا مباشرةً لـ:

> `NO PATCH BEFORE RECONCILIATION`

الموجود في التكليف. 

### ما تم فعليًا

* تم الوصول إلى Branch `rescue/manual-vouchers-inventory-core`.
* تمت مراجعة وثيقة التكليف كاملة: **676 سطرًا**. 
* تمت مراجعة Architecture constraints.
* تمت مراجعة الـ7 وثائق المطلوبة.
* تمت مراجعة Current CREATE/SEND/RECEIVE/COMPLETE/CANCEL.
* تمت مراجعة Original المقابلة.
* تمت مراجعة Production RPC evidence، مع إعطاء V4 الأولوية.
* تمت مراجعة Migration المستهدفة.
* تمت مقارنة Production vs Current vs Original vs Migration vs Architecture.
* **لم يتم تعديل أي ملف.**
* **لم يتم تنفيذ SQL.**
* **لم يتم تغيير Production.**

## التقييم الذاتي

**هل لدي يقين كافٍ؟**
نعم بالنسبة إلى نقاط التعارض التي ذكرتها؛ كل منها لها دليل مباشر.

**هل هناك تخمين؟**
لا.

**هل تم افتراض Target لـDirectSale أو DirectReturn؟**
لا. تم تصنيفهما `TARGET DECISION REQUIRED` لأن الأدلة نفسها متعارضة.

**هل `completed_by` مشكلة مثبتة؟**
نعم، **100% من الأدلة المتاحة** تثبت أن Production RPC تكتبه بينما Production Schema لا يحتويه.

**هل نستطيع إعطاء GO؟**
**لا.**

**الثقة في نتيجة الـReconciliation الحالية: 97%.**

والقرار الصحيح في هذه اللحظة هو: **NO GO — Target Lifecycle Reconciliation Required Before Patch.**
